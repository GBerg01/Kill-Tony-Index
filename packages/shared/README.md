# Shared package

Shared TypeScript types, constants, and helpers used across web and worker apps.

Planned exports:

- API response types
- Database entity types
- Constants for rating labels and leaderboard filters

Current exports:
- `types.ts` for core entity types.
- `api.ts` for shared API response wrappers.
